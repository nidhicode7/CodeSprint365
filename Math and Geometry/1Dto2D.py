#Here we are converting one dimensional array to 2 dimensional array Which is a leetcode problem  2022 and what we have to do is first

def convert_1d_to_2d(arr, rows, cols):
if m*n!=len(original):
    return []
result=[]
else:
  for i in range (0,len(original),cols):
    result.append(arr[i:i+cols])
  return result
    
    
  
